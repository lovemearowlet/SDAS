#include <stdio.h>
#include <stdlib.h>

#include "config.c"

int update(char **pkg) {//update
	if (dis == 0) {//gentoo
		if (sod == 0) {
			//gentoo with sudo
		}
		if (sod == 1) {
			//gentoo with doas
		}
	}
	if (dis == 1) {//arch
		if (sod == 0) {
			//arch with sudo
		}
		if (sod == 1) {
			//arch with doas
		}
	}
}

int install(char **pkg) {//install
	if (dis == 0) {//gentoo
		if (sod == 0) {
			//gentoo with sudo
		}
		if (sod == 1) {
			//gentoo with doas
		}
	}
	if (dis == 1) {//arch
		if (sod == 0) {
			//arch with sudo
		}
		if (sod == 1) {
			//arch with doas
		}
	}
}

int uninstall(char **pkg) {//uninstall
	if (dis == 0) {//gentoo
		if (sod == 0) {
			//gentoo with sudo
		}
		if (sod == 1) {
			//gentoo with doas
		}
	}
	if (dis == 1) {//arch
		if (sod == 0) {
			//arch with sudo
		}
		if (sod == 1) {
			//arch with doas
		}
	}
}
