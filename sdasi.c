#include <stdio.h>
#include <assert.h>
#include <stdlib.h>

char su[] = "sudo";
//alternate names: doas, sudo
char distro[] = "Gentoo";
// alternate names: Gentoo Arch Debian
//install thing and main funtion

void install(char *pkg) {
    if (distro = "Gentoo") {
        //do the install command
    }
//    if (distro = "Arch") {
//        //arch install commadn
//    }
//    if (distro = "Debian") {
//        //debian install command 
//    }
}

